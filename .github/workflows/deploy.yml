# Auto-generated by AutoRollback Tool
# Generated on: 2025-11-04T10:18:23.835560Z

name: Deploy to Azure

on:
  push:
    branches: [ main ]
  workflow_dispatch:
    inputs:
      environment:
        description: 'Deployment environment'
        required: false
        default: 'Dev'

env:
  AZURE_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
  RESOURCE_GROUP: ${{ secrets.AZURE_RESOURCE_GROUP }}
  
jobs:
  build:
    name: Build Application
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Build application
        run: npm run build

      
      # Tests skipped (runTests=false)
      
      - name: Determine build output directory
        id: build-path
        run: |
          # Check for common build output directories
          if [ -d "./build" ]; then
            echo "path=./build" >> $GITHUB_OUTPUT
            echo "✅ Found build output in ./build"
          elif [ -d "./dist" ]; then
            echo "path=./dist" >> $GITHUB_OUTPUT
            echo "✅ Found build output in ./dist"
          elif [ -d "./target" ]; then
            echo "path=./target" >> $GITHUB_OUTPUT
            echo "✅ Found build output in ./target"
          elif [ -d "./publish" ]; then
            echo "path=./publish" >> $GITHUB_OUTPUT
            echo "✅ Found build output in ./publish"
          elif [ -d "./build/libs" ]; then
            echo "path=./build/libs" >> $GITHUB_OUTPUT
            echo "✅ Found build output in ./build/libs"
          else
            echo "❌ No build output directory found!"
            echo "Checked: ./build, ./dist, ./target, ./publish, ./build/libs"
            exit 1
          fi

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: build-output
          path: ${{ steps.build-path.outputs.path }}
          retention-days: 1

  deploy:
    name: Deploy to Azure
    needs: build
    runs-on: ubuntu-latest

    steps:
      - name: Download build artifacts
        uses: actions/download-artifact@v4
        with:
          name: build-output
          path: ./artifact

      - name: Extract publish profile credentials
        id: publish-creds
        run: |
          # Extract username and password from publish profile XML
          PUBLISH_PROFILE='${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE }}'

          # Extract the first publishProfile (MSDeploy) using sed for compatibility
          # Extract userName
          USERNAME=$(echo "$PUBLISH_PROFILE" | sed -n 's/.*userName="\([^"]*\)".*/\1/p' | head -1)
          # Extract userPWD
          PASSWORD=$(echo "$PUBLISH_PROFILE" | sed -n 's/.*userPWD="\([^"]*\)".*/\1/p' | head -1)

          # Set as output for next step (mask password in logs)
          echo "username=$USERNAME" >> $GITHUB_OUTPUT
          echo "::add-mask::$PASSWORD"
          echo "password=$PASSWORD" >> $GITHUB_OUTPUT

          # Verify we got credentials
          if [ -z "$USERNAME" ] || [ -z "$PASSWORD" ]; then
            echo "❌ Failed to extract credentials from publish profile"
            echo "Publish profile format may be invalid"
            exit 1
          fi

          echo "✅ Successfully extracted credentials for user: $USERNAME"

      - name: Create deployment package
        run: |
          cd artifact
          zip -r ../deploy.zip .
          cd ..
          echo "📦 Deployment package created: $(du -h deploy.zip)"

      - name: Deploy to Azure App Service via Kudu API
        env:
          USERNAME: ${{ steps.publish-creds.outputs.username }}
          PASSWORD: ${{ steps.publish-creds.outputs.password }}
        run: |
          echo "🚀 Deploying to customer-portal-rf49dx..."

          # Use Kudu ZipDeploy API
          # This is the underlying API that azure/webapps-deploy uses
          KUDU_URL="https://customer-portal-rf49dx.scm.azurewebsites.net/api/zipdeploy"

          # Deploy using basic auth from publish profile
          RESPONSE=$(curl -X POST \
            -u "$USERNAME:$PASSWORD" \
            --data-binary @deploy.zip \
            -H "Content-Type: application/octet-stream" \
            -w "\n%{http_code}" \
            "$KUDU_URL")

          # Extract HTTP status code
          HTTP_CODE=$(echo "$RESPONSE" | tail -n1)

          if [ "$HTTP_CODE" -eq 200 ] || [ "$HTTP_CODE" -eq 202 ]; then
            echo "✅ Deployment successful! Status: $HTTP_CODE"
          else
            echo "❌ Deployment failed with status: $HTTP_CODE"
            echo "Response: $RESPONSE"
            exit 1
          fi

          echo "🎉 Application deployed to https://customer-portal-rf49dx.azurewebsites.net"

      
      - name: Health Check
        run: |
          echo "⏳ Waiting 30 seconds for application to start..."
          sleep 30
          
          echo "🔍 Performing health check..."
          MAX_RETRIES=10
          RETRY_COUNT=0
          
          while [ $RETRY_COUNT -lt $MAX_RETRIES ]; do
            HTTP_STATUS=$(curl -s -o /dev/null -w "%{http_code}" ${{ secrets.APP_URL }}/healthz || echo "000")
            
            if [ "$HTTP_STATUS" = "200" ]; then
              echo "✅ Health check passed! Application is healthy."
              exit 0
            fi
            
            echo "⚠️  Health check failed (HTTP $HTTP_STATUS). Retry $((RETRY_COUNT + 1))/$MAX_RETRIES..."
            sleep 10
            RETRY_COUNT=$((RETRY_COUNT + 1))
          done
          
          echo "❌ Health check failed after $MAX_RETRIES attempts!"
          echo "🔄 Triggering automatic rollback..."
          exit 1
      
      - name: Trigger Rollback on Failure
        if: failure()
        uses: actions/github-script@v6
        with:
          script: |
            await github.rest.actions.createWorkflowDispatch({
              owner: context.repo.owner,
              repo: context.repo.repo,
              workflow_id: 'rollback.yml',
              ref: 'main',
              inputs: {
                reason: 'Deployment health check failed',
                failed_run_id: context.runId.toString()
              }
            });
