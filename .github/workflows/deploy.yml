# Auto-generated by AutoRollback Tool
# Generated on: 2025-11-04T14:00:17.595621Z

name: Deploy to Azure

on:
  push:
    branches: [ main ]
  workflow_dispatch:
    inputs:
      environment:
        description: 'Deployment environment'
        required: false
        default: 'Dev'

env:
  AZURE_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
  RESOURCE_GROUP: ${{ secrets.AZURE_RESOURCE_GROUP }}
  
jobs:
  build:
    name: Build Application
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Build application
        run: npm run build

      
      # Tests skipped (runTests=false)
      
      - name: Determine build output directory
        id: build-path
        run: |
          # Check for common build output directories
          if [ -d "./build" ]; then
            echo "path=./build" >> $GITHUB_OUTPUT
            echo "✅ Found build output in ./build"
          elif [ -d "./dist" ]; then
            echo "path=./dist" >> $GITHUB_OUTPUT
            echo "✅ Found build output in ./dist"
          elif [ -d "./target" ]; then
            echo "path=./target" >> $GITHUB_OUTPUT
            echo "✅ Found build output in ./target"
          elif [ -d "./publish" ]; then
            echo "path=./publish" >> $GITHUB_OUTPUT
            echo "✅ Found build output in ./publish"
          elif [ -d "./build/libs" ]; then
            echo "path=./build/libs" >> $GITHUB_OUTPUT
            echo "✅ Found build output in ./build/libs"
          else
            echo "❌ No build output directory found!"
            echo "Checked: ./build, ./dist, ./target, ./publish, ./build/libs"
            exit 1
          fi

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: build-output
          path: ${{ steps.build-path.outputs.path }}
          retention-days: 1

  deploy:
    name: Deploy to Azure
    needs: build
    runs-on: ubuntu-latest

    steps:
      - name: Download build artifacts
        uses: actions/download-artifact@v4
        with:
          name: build-output
          path: ./artifact

      - name: Extract publish profile credentials
        id: publish-creds
        run: |
          # Extract username and password from publish profile XML
          PUBLISH_PROFILE='${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE }}'

          # Extract the first publishProfile (MSDeploy) using sed for compatibility
          # Extract userName
          USERNAME=$(echo "$PUBLISH_PROFILE" | sed -n 's/.*userName="\([^"]*\)".*/\1/p' | head -1)
          # Extract userPWD
          PASSWORD=$(echo "$PUBLISH_PROFILE" | sed -n 's/.*userPWD="\([^"]*\)".*/\1/p' | head -1)

          # Set as output for next step (mask password in logs)
          echo "username=$USERNAME" >> $GITHUB_OUTPUT
          echo "::add-mask::$PASSWORD"
          echo "password=$PASSWORD" >> $GITHUB_OUTPUT

          # Verify we got credentials
          if [ -z "$USERNAME" ] || [ -z "$PASSWORD" ]; then
            echo "❌ Failed to extract credentials from publish profile"
            echo "Publish profile format may be invalid"
            exit 1
          fi

          echo "✅ Successfully extracted credentials for user: $USERNAME"

      - name: Create deployment package
        run: |
          cd artifact
          zip -r ../deploy.zip .
          cd ..
          echo "📦 Deployment package created: $(du -h deploy.zip)"

      - name: Deploy to Azure App Service via Kudu API
        env:
          USERNAME: ${{ steps.publish-creds.outputs.username }}
          PASSWORD: ${{ steps.publish-creds.outputs.password }}
        run: |
          echo "🚀 Deploying to customer-portal-gec5jg..."

          # Use Kudu ZipDeploy API
          # The publish profile uses SCM deployment credentials
          KUDU_URL="https://customer-portal-gec5jg.scm.azurewebsites.net/api/zipdeploy"

          echo "📍 Deploying to: $KUDU_URL"
          echo "👤 Using credentials for: $USERNAME"

          # Deploy using basic auth from publish profile
          # Add -v for verbose output to debug auth issues
          HTTP_CODE=$(curl -X POST \
            -u "$USERNAME:$PASSWORD" \
            --data-binary @deploy.zip \
            -H "Content-Type: application/octet-stream" \
            -H "Cache-Control: no-cache" \
            -w "%{http_code}" \
            -o /tmp/deploy_response.txt \
            -s \
            "$KUDU_URL")

          echo "HTTP Status Code: $HTTP_CODE"

          if [ "$HTTP_CODE" -eq 200 ] || [ "$HTTP_CODE" -eq 202 ]; then
            echo "✅ Deployment successful! Status: $HTTP_CODE"
            echo "🎉 Application deployed to https://customer-portal-gec5jg.azurewebsites.net"
          else
            echo "❌ Deployment failed with status: $HTTP_CODE"
            echo "Response body:"
            cat /tmp/deploy_response.txt

            if [ "$HTTP_CODE" -eq 401 ]; then
              echo ""
              echo "Authentication failed. Possible causes:"
              echo "1. Publish profile credentials expired or invalid"
              echo "2. Basic authentication not enabled on App Service"
              echo "3. Username/password extraction failed"
              echo ""
              echo "Checking if we can access Kudu without deploy..."
              curl -u "$USERNAME:$PASSWORD" -s -o /dev/null -w "Kudu root status: %{http_code}\n" "https://customer-portal-gec5jg.scm.azurewebsites.net/"
            fi
            exit 1
          fi

      
      - name: Health Check
        id: health-check
        continue-on-error: true
        run: |
          echo "⏳ Waiting 30 seconds for application to start..."
          sleep 30

          echo "🔍 Performing health check..."
          MAX_RETRIES=10
          RETRY_COUNT=0
          HEALTH_PASSED=false

          # For static sites, check root path (/) instead of /health endpoint
          # Accept 200 (OK) or 304 (Not Modified) as success
          while [ $RETRY_COUNT -lt $MAX_RETRIES ]; do
            HTTP_STATUS=$(curl -s -o /dev/null -w "%{http_code}" ${{ secrets.APP_URL }}/healthz || echo "000")

            if [ "$HTTP_STATUS" = "200" ] || [ "$HTTP_STATUS" = "304" ]; then
              echo "✅ Health check passed! Application is healthy (HTTP $HTTP_STATUS)."
              HEALTH_PASSED=true
              break
            fi

            echo "⚠️  Health check failed (HTTP $HTTP_STATUS). Retry $((RETRY_COUNT + 1))/$MAX_RETRIES..."
            sleep 10
            RETRY_COUNT=$((RETRY_COUNT + 1))
          done

          if [ "$HEALTH_PASSED" = "false" ]; then
            echo "❌ Health check failed after $MAX_RETRIES attempts!"
            echo "health_failed=true" >> $GITHUB_OUTPUT
            exit 1
          else
            echo "health_failed=false" >> $GITHUB_OUTPUT
          fi

      - name: Check if Rollback is Possible
        id: rollback-check
        if: steps.health-check.outputs.health_failed == 'true'
        run: |
          echo "🔍 Checking if rollback is possible..."

          # Check if this is the first deployment (no previous deployment slot)
          # For Azure App Service, check if there are previous deployments
          DEPLOYMENT_COUNT=$(az webapp deployment list \
            --resource-group ${{ secrets.AZURE_RESOURCE_GROUP }} \
            --name customer-portal-gec5jg \
            --query "length(@)" \
            --output tsv 2>/dev/null || echo "0")

          if [ "$DEPLOYMENT_COUNT" -le "1" ]; then
            echo "⚠️  This appears to be the first deployment - no previous version to rollback to"
            echo "rollback_possible=false" >> $GITHUB_OUTPUT
            echo "::warning::Health check failed on first deployment. No rollback performed."
            exit 0
          else
            echo "✅ Previous deployment found - rollback is possible"
            echo "rollback_possible=true" >> $GITHUB_OUTPUT
          fi

      - name: Trigger Rollback on Failure
        if: steps.rollback-check.outputs.rollback_possible == 'true'
        uses: actions/github-script@v6
        with:
          script: |
            console.log('🔄 Triggering automatic rollback...');
            await github.rest.actions.createWorkflowDispatch({
              owner: context.repo.owner,
              repo: context.repo.repo,
              workflow_id: 'rollback.yml',
              ref: 'main',
              inputs: {
                reason: 'Deployment health check failed',
                failed_run_id: context.runId.toString()
              }
            });
